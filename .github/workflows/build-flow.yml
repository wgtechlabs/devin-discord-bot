name: Build Flow

on:
  pull_request:
    branches: [dev, main]
  push:
    branches: [dev, main]
  workflow_dispatch:

concurrency:
  group: build-flow-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-flow:
    uses: wgtechlabs/build-flow-action/.github/workflows/app.yml@8172bda0cd19bf31207968da4671a9a52986d004 # v0.1.7
    secrets: inherit
    with:
      ci-profile: auto
      ci-matrix-versions: '["22","24","26"]'
      codeql-build-mode: none
      enable-package: false
      enable-container: true
      container-registry: both
